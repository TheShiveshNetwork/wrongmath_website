import LoginForm from "@/components/forms/LoginForm"

type Props = {}

const Login = (props: Props) => {
    return (
        <div>
            <LoginForm />
        </div>
    )
}

export default Login